/**
 * LogIn component when user not logged in. Gives them a button and overlay for authn/z.
 * @returns {ReactNode} Returns login component.
 */

function LogIn() {
  
  return (
    <p>Log In/Register</p>
  );
}

export default LogIn;